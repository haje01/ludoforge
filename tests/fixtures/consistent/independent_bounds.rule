# 정합: 독립 변수들의 선언 범위가 룰에 의해 봉쇄되지 않는다.
domain:
  variables:
    gold:  { type: int, min: 0, max: 1000 }
    level: { type: int, min: 1, max: 50 }

constraints:
  - id: reward_cap
    when: "level >= 10"
    then: "gold <= 1000"
