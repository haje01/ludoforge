# 실수 변수의 선언 상한이 룰로 봉쇄되는 모순 (LRA, D9 끝점 검사)
# drop_rate는 [0,1]로 선언됐지만 룰이 0.3 이하로 막아 선언 max=1.0에 도달할 수 없다.
domain:
  variables:
    drop_rate: { type: real, min: 0, max: 1 }

constraints:
  - id: rate_capped
    author: planner_loot
    desc: "드롭 확률은 0.3을 넘지 않는다"
    then: "drop_rate <= 0.3"
